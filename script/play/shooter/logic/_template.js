(function() {
    "use strict";
    var p  = function(spec) {this.initialize(spec);};
    // Object Name
    MyDef.logic.Sample = p;
    // Initialize
    p.prototype.initialize = function(spec) {
        //initalize
    };
    // Method
    p.prototype.execute = function(owner) {
    };
}());
