(function() {
    "use strict";
    var p  = function(spec) {this.initialize(spec);};
    // Object Name
    MyDef.Sample = p;
    // Initialize
    p.prototype.initialize = function(spec) {
    };
}());
